# 개인 보안 설정 (제스처/비상키) — 절대 커밋 금지
config.local.json

# 런타임 로그 (제스처 기록 포함)
protocol.log

# 침입자 스냅샷 (얼굴 사진 — 절대 커밋 금지)
intruders/

# 단일 실행 락
.security_protocol.lock

# 환경
venv/
__pycache__/
*.pyc
.DS_Store

# 모델 (setup.sh가 다운로드)
models/

# 머신 종속 파일 (install-autostart.sh가 생성)
*.plist

# 자동 시작 로그
autostart.log

# 생성되는 앱 번들
SecurityProtocol1.app/
.app-pythonpath
